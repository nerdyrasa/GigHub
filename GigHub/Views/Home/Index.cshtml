@model GigHub.ViewModels.GigsViewModel
@{
    ViewBag.Title = "Upcoming Gigs";
}

<h2>All Upcoming Gigs</h2>

<div class="container">
    @foreach (var gig in Model.UpcomingGigs)
    {

        <div class="row">
            <div class="col-sm-2">
                @gig.DateTime.ToString("MMM d, yyyy")
            </div>
            <div class="col-sm-2">
                <a href="" class="href"></a>
                @gig.Artist.Name
            </div>
            <div class="col-sm-pull-2">
                @if (Model.ShowActions)
                {
                    <button data-gig-id="@gig.Id"
                            class="btn
                                @(Model.Attendances.Contains(gig.Id) ? "btn-info" : "btn-default")
                                btn-sm js-toggle-attendance">
                        @(Model.Attendances.Contains(gig.Id) ? "Going " : "Going?")
                    </button>
                }
            </div>
            <div class="col-sm-pull-6">
                @*@if (Model.ShowActions)
                    {
                        <button data-followee-id="@gig.ArtistId" class="btn
                                @(Model.Followings.Contains(gig.ArtistId) ? "btn-info" : "btn-default")
                                 js-toggle-following">
                            @(@Model.Followings.Contains(gig.ArtistId) ? "Following" : "Follow")
                        </button>
                    }*@
            </div>
        </div>

    }
</div>

@section scripts
{
    @Scripts.Render("~/bundles/gigs")
}